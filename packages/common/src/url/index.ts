// Generic functions for programatically creating an URL path from an internal URL object (UrlObject)
export * from './get-path'

// Navigate hook which takes an URL object (UrlObject) and pushes or replaces history to a new URL path
// export * from './navigate'

// Query hook to convert the URL path's search query to an internal URL object (UrlObject)
// To get the projectId and documentId from URL path we can use default hooks from react-router-dom
export * from './query'
